# Email-Spam-Prediction
Builds a machine learning system that automatically classifies emails as Spam or Not Spam (Ham). You used real email/SMS data, applied NLP techniques to clean and transform text, trained multiple ML models, and deployed it as a web app using Streamlit.
